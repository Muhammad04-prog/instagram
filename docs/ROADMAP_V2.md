# ROADMAP v2 — Миграция на новый backend (NestJS)

**Новый API:** `https://backend-instagram-a4k6.onrender.com/api`
**Swagger:** `/api/docs-json` — **230 endpoints** (19.07.2026, было 190) против 57 у softclub.
Конверт ответа тот же: `{ data, errors, statusCode }` → `lib/axios.ts` unwrap **остаётся**.

Этот бэкенд написан как **замена** softclub: в его же описаниях полей стоят ссылки на наши баги
(`gender` — «Симметричный enum: что отправили — то и вернётся (баг softclub #12)`). Все 5 🔴-багов
из `BACKEND_BUGS.md` здесь закрыты, плюс появилось то, чего в API не было и что мы честно
не делали: уведомления, запросы на переписку, архив историй, «Добавить место», дата рождения.

Правило прежнее: **одна фаза = одна сессия**, строго сверху вниз, `[x]` по готовности.

---

## Где мы сейчас — 221/230

Числа берутся из `docs/API_MAP_V2.md` (генерируется, не проставляется руками). «Проведён» = endpoint
**достижим человеком**: вызов из `components`/`app` напрямую или через хук, который там используется.
Готовый хук без экрана **не считается**.

⚠️ **Ни один из 166 не подтверждён живым ответом** — БД нерабочая всю миграцию и **остаётся**
такой: `/health` рапортует `database: up`, но любой запрос в базу → 500 `DATABASE_ERROR`
(проверено curl'ом 17.07.2026, см. § Блокеры).

📌 17.07.2026 swagger обновлён: 170 → **190**, хост `kvv4` → **`a4k6`**. Новые 20 endpoint'ов
не проведены и разложены по Фазам 22–25 внизу.

📌 **19.07.2026 swagger обновлён снова: 190 → 230** — см. § Swagger v4 внизу. В эту же сессию
закрыты **locations 6/6** (`GET /locations/{id}/posts` — грид на странице места, которого не было
никогда) и **settings 5/5** (новый тег: общие настройки + аккаунты с ограничениями — оживило пять
экранов, которые раньше были чистым `useState`-муляжом с подписью «скоро»: `TagsMentionsSettings`,
`SharingSettings`, `CommentsSettings`, `HiddenWordsSettings` (список слов, не переключатели-категории),
`NotificationsSettings`, `MessagesSettings` (только `whoCanMessage`), плюс «Ограничить» в меню профиля
и список в `/settings/restricted`).

📌 Следующая сессия того же дня закрыла **Фазу 27 — Посты pro: 14/14** — все новые endpoint'ы
тега `posts` подключены. `posts` теперь **35/36**: единственный `[ ]` — тот же намеренный дубль
`GET /posts`, что и раньше (§ «Единственное сознательное исключение» в `API_MAP_V2.md`). См.
§ Фаза 27 ниже.

📌 Третья сессия того же дня закрыла **Фазу 28 — Истории pro: 7/7** — `stories` теперь **19/19**.
См. § Фаза 28 ниже.

📌 Четвёртая сессия того же дня закрыла **Фазу 26 — Безопасность: 7/7** — `auth` теперь **18/18**.
См. § Фаза 26 ниже. По пути найдена и исправлена мелкая слепая зона в `scripts/gen-api-map.js`:
server-side `fetch` с query-строкой (`?rt=…`) не нормализовался и не совпадал со своим же путём
в Swagger — теперь query отрезается перед сравнением.

Итог четырёх сессий: **169/190 → 204/230**.

📌 Пятая сессия того же дня закрыла **Фазу 29 — Групповые чаты: 5/5** — `chats` теперь **24/33**.
См. § Фаза 29 ниже.

Итог пяти сессий: **169/190 → 209/230**.

📌 Шестая сессия того же дня закрыла **Фазу 30 — Приватность чата: 3/3** — `chats` теперь **27/33**
(оставшиеся 6 — звонки/ICE/сокет, Фаза 22, заблокирована). См. § Фаза 30 ниже.

Итог шести сессий: **169/190 → 212/230**.

📌 Седьмая сессия того же дня закрыла **Фазу 25 (хвосты-2): 7/7** — `live` теперь **20/20**,
`music` **10/10**, `notes` **10/10**, `profile` **16/16** — все четыре тега полностью проведены.
См. § Фаза 25 (хвосты-2) ниже.

Итог семи сессий: **169/190 → 221/230**.

📌 Восьмая сессия того же дня закрыла **Фазу 31 — Единая лента: 1/1** — `feed` теперь **1/1**.
`GET /feed` и `GET /posts/feed` оказались дублями друг друга (тот же `FeedDto`, те же параметры,
то же описание ранжирования) — `useFeed` переключён на новый `/feed`, `/posts/feed` стал вторым
задокументированным дублем (после `GET /posts`) в `API_MAP_V2.md`. Заодно нашли и закрыли реальный
пробел: `FeedDto.allCaughtUp`/`suggested` существовали с самого начала, но нигде не читались —
теперь лента показывает «Вы всё посмотрели» и рекомендованные посты после конца подписок. См.
§ Фаза 31 ниже.

Итог восьми сессий: **169/190 → 221/230** (без изменения числа — один дубль сменил другой, но
экран стал честнее). Осталось 9: `GET /posts` + `GET /posts/feed` (два намеренных пропуска-дубля),
сокет + звонки (7, Фаза 22, заблокирована именами событий).

📌 **Девятая сессия (тот же день) — не проводка, а ревизия.** `docs/BACKEND_PROMPT.md` полностью
пересмотрен: §5, §6, §7, §9, §11–13 отмечены закрытыми (бэкенд их закрыл, но документ этого не
отражал), добавлены §14 (черновики) и §15 (`viewOnce` при отправке) — те же два пробела, что уже
описаны в `BACKEND_REQUEST.md` §6 и §8. Заодно найдены и закрыты **три поля, которые генерировались
в типы, но нигде не читались** — тот же паттерн, что `allCaughtUp`/`suggested` в Фазе 31:

- `NotificationDto.postThumbUrl` (с 17.07.2026) — миниатюра поста в строке уведомления
- `PostDto.remixOf` — плашка «Remix of @author» на reel
- `PostDto.collaborators` — подпись «Совместно с …» под именем автора
- `PostDto.commentsDisabled` — сам тумблер (Фаза 27) переключался, но `CommentForm` игнорировал
  его и показывался всегда; теперь скрывается на всех трёх экранах (`PostCard`, `PostDetail`,
  `ReelCard`)

| Тег           |    Готово | Фаза  | Что осталось                                                |
| ------------- | --------: | ----- | ----------------------------------------------------------- |
| auth          | **11/11** | 12 ✅ | —                                                           |
| users         |     12/13 | 13    | `by-username/{userName}` — новый (Фаза 25)                  |
| follow        | **11/11** | 13 ✅ | —                                                           |
| posts         |     21/22 | 14    | `GET /posts` — не нужен: `/search/explore` даёт ту же сетку |
| notifications |   **5/5** | 15 ✅ | —                                                           |
| stories       | **12/12** | 16 ✅ | музыка/стикеры в истории (не endpoint)                      |
| close-friends |   **3/3** | 16 ✅ | —                                                           |
| locations     |       5/6 | 10/13 | `{id}/posts` — новый (Фаза 25)                              |
| highlights    |   **5/5** | 16 ✅ | —                                                           |
| profile       |     14/15 | 13    | `me/collections` — новый (Фаза 25)                          |
| chats         |     19/30 | 17 🚧 | группы (Фаза 23), звонки + сокет (Фаза 22)                  |
| music         |       6/9 | 18    | внешний каталог `online/*` — новый (Фаза 24)                |
| spotify       |   **3/3** | 18 ✅ | —                                                           |
| notes         |       8/9 | 18    | `GET /notes/{id}` — новый (Фаза 25)                         |
| search        |   **4/4** | 19 ✅ | —                                                           |
| upload        |   **2/2** | 11 ✅ | —                                                           |
| verification  |   **4/4** | 20 ✅ | —                                                           |
| admin         |   **4/4** | 20 ✅ | —                                                           |
| live          |     16/20 | 21    | видео (LiveKit) 🔴 + заявки: разблокированы (Фаза 25)       |
| socket        |       0/1 | 22    | `POST /socket/ticket` — ключ к звонкам и realtime           |
| health        |   **1/1** | 19 ✅ | —                                                           |

## Что ломается (главное)

| Что             | Было (softclub)                     | Стало (NestJS)                                                                                   |
| --------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------ |
| Базовый URL     | `instagram-api.softclub.tj`         | `backend-instagram-a4k6.onrender.com/api`                                                        |
| Роуты           | `/Post/get-posts` (Pascal, глаголы) | `/posts`, `/posts/feed` (REST, ресурсы)                                                          |
| Auth            | 1 access-токен                      | **access + refresh** (ротация) + `role`, `isVerified`                                            |
| Login           | `userName` + `password`             | `login` = userName **ИЛИ** email **ИЛИ** phone                                                   |
| Reset password  | 2 шага (DELETE!)                    | 4 шага: forgot → **код 6 цифр на email** → verify-code → resetToken                              |
| Register        | 5 полей                             | + `dob` (**обязателен**) + `phone` (опц.) → блок «Дата рождения» из img4 **теперь делаем**       |
| id пользователя | `int`                               | **`string` (uuid)** — ломает все типы и роуты                                                    |
| Пагинация       | `page`/`limit`                      | **`cursor`/`limit`** — все infinite-списки                                                       |
| `gender`        | читался `"Male"`, писался `0│1` 🔴  | enum `MALE│FEMALE│OTHER│HIDDEN` симметрично                                                      |
| Медиа поста     | `images: string[]`                  | `media: [{ url, type: IMAGE│VIDEO, width, height, duration, thumbUrl, filter }]`                 |
| Пост            | без автора                          | `author`, `location`, `music`, `taggedUsers`, `hashtags`, `isLiked`, `isFavorited`, `isArchived` |
| Story           | «видел ли я» не отдавал 🔴          | `isViewed`, `isLiked`, `likesCount` — **localStorage-костыль из Фазы 6 удаляем**                 |
| Чат             | без last message / unread 🔴        | `lastMessage`, `lastMessageAt`, `unreadCount`, `isOnline`, `isMuted`, `theme`                    |
| Realtime        | хаба нет → поллинг 5 сек 🔴         | **сокет** (звонки/эфиры сигналятся через него)                                                   |
| Комментарии     | `userName` всегда `null` 🔴         | автор приходит; + лайки и **ответы (ветки)**                                                     |

## Что чинится само (закрываем `BACKEND_BUGS.md`)

`delete-user` 403 → `DELETE /users/me` (soft-delete) · `update-Location` 400 AutoMapper → `PUT /locations/{id}` ·
`delete-message` без OwnerGuard → **OwnerGuard есть** · `delete-user-image-profile` ломал login → `DELETE /profile/avatar` ·
нет unread/realtime → есть оба · нет истории с `createdAt` → **`createdAt` есть**.

---

## Фаза 11 — Фундамент нового API (ядро; без неё ничего не работает) ✅

- [x] `.env` + `lib/constants.ts` → `https://backend-instagram-a4k6.onrender.com/api`; `next.config.ts` — `remotePatterns`
- [x] `types/api.gen.ts` (openapi-typescript) + `types/api.types.ts` — **123 DTO плоскими именами**; `npm run api:types`
- [x] `lib/auth-tokens.ts` + прокси — **refresh-флоу**: токен обновляется **до** запроса (по `exp`), гонка сводится в один промис (`inFlight` по значению токена)
- [x] `app/api/auth/session/route.ts` — пара токенов в httpOnly; личность из **`GET /auth/me`**, а не из парсинга JWT
- [x] `lib/cursor.ts` — `nextCursor()` / `cursorParams()`, единый `getNextPageParam` для всех списков
- [x] Сервисы переписаны и переименованы по тегам: `auth`, `profile`, `follow`, `post`, `story`, `chat`, `user`, `location`, **`upload`** (новый)
- [x] Все 11 хуков на курсорах и новых DTO
- [x] Удалены: `store/story.store.ts`, `types/{user,location,story}.types.ts`, `getChatPeer()`, `useProfileLite()`, `GENDER_VALUE`
- [x] Компоненты переведены на новые DTO (135 ошибок типов → 0)
- [x] `build` / `lint` / `typecheck` — зелёные

> Заметки Фазы 11:
>
> - ⚠️ **БД лежала всю фазу** (`database/redis/storage: down`), поэтому правило «живой API авлотар»
>   применить было не к чему: все DTO взяты из Swagger и **не сверены с реальностью**. Как только БД
>   поднимут — прогнать регистрацию → логин → ленту и сверить (особенно `/auth/me`: в Swagger он
>   описан как «текущий пользователь + профиль», и `session/route.ts` принимает **оба** варианта —
>   голый `AuthUserDto` и конверт `{ user, profile }`).
> - ⚠️ **Хост объектного хранилища (MinIO) неизвестен** — ни одного реального URL медиа мы не видели.
>   В `next.config.ts` пока только хост API; первый же настоящий URL нужно туда добавить, иначе
>   `<Image>` его не отрисует.
> - **Refresh-токены ротируются**, поэтому обновление идёт **проактивно** (по `exp` с запасом 30 сек),
>   а не по 401: тело запроса стримится (multipart), а поток нельзя переиграть — запрос должен быть
>   верным с первого раза. Параллельные запросы делят один промис обновления, иначе проигравший
>   предъявил бы уже отозванный токен и вылетел бы в логаут.
> - **Курсор выводится на клиенте**: списки отдают голый массив, а `cursor` = id последнего элемента
>   («курсор последнего элемента предыдущей страницы» — Swagger, `GET /users`). Короткая страница = конец.
> - ⚠️ **`/stories/{id}/viewers` не пагинируется**: у `StoryViewerDto` нет своего `id`, а курсор — это
>   именно id последней строки. Берём разом лимит 50; больше 50 зрителей — обрежется.
> - Фаза 12 частично зашла сюда — иначе типы не собирались: логин по `login` (userName/email/phone),
>   **блок «Дата рождения» из img4** (`DobSelect`, три дропдауна — свёрстан по скриншоту),
>   **live-проверка userName** (`UsernameField`), **OTP-флоу** (`CodeInput` + таймер 60 сек).
>   Остальное по Фазе 12 (429-тексты, logout-ревокация в UI) — там же.
> - Добавлен токен `--ig-success` (light + dark): «имя свободно» нужно зелёным, а раньше success-состояний
>   в API просто не было. HEX по-прежнему только в `globals.css`.
> - `/reset-password` теперь **редирект** на `/forgot-password`: писем со ссылкой больше нет, есть код.
> - Проверено вживую (Playwright, dark + light + /ru): `/register` совпадает с img4 (День/Месяц/Год),
>   `/login` — «Телефон, имя пользователя или эл. адрес», OTP-боксы двигают фокус, «Отправить ещё раз
>   через 59 с» тикает. Ошибок в консоли нет. Дальше страниц пройти нельзя — БД лежит.
> - 📐 **Метрики auth перемерены по пикселям** (canvas-скан границ инпутов в img4/img5/img6/img7,
>   DPR 1.25), а не на глаз:
>   - инпут **60px** (полоса 74px / 1.25) — было `h-14` = 56
>   - кнопка **44px** (полоса 55px / 1.25) — было `h-12` = 48
>   - колонка регистрации **560px** (края инпута 483→1183 = 700 / 1.25) — было `max-w-[700px]`
>   - «Дата рождения»: три бокса **176px** с зазором **16px** (176·3 + 16·2 = 560 ✓)
>     Проверено в браузере: реальный рендер = 560×60 инпут, 176×60 бокс, зазор 16, кнопка 560×44.

## Фаза 12 — Auth v2 (11 endpoints) ✅

- [x] Логин по **email / телефону / userName** (одно поле `login`)
- [x] Регистрация: **«Дата рождения»** (img4) + телефон + **live-проверка userName** (`check-username`, debounce)
- [x] Сброс пароля: 3 экрана — email → **6-значный код** (`CodeInput`: авто-фокус, вставка, Backspace) → новый пароль
- [x] `logout` (отзыв refresh на сервере), `GET /auth/me`
- [x] `hooks/useApiError.ts` — 429 / 401 / 409 / сеть человеческим текстом, а не «Request failed»
- [x] `docs/API_MAP_V2.md` — **генерируется** скриптом (`node scripts/gen-api-map.js`), проводка меряется, а не заявляется
- ✅ Готово: **11/11 auth** endpoint'ов проведены в UI

> Заметки Фазы 12:
>
> - Пункты 1–4 были сделаны ещё в Фазе 11 (без них не собирались типы); здесь закрыт остаток —
>   тексты ошибок, карта API и **починка реального бага**.
> - 🐛 **`DobSelect` не работал вообще** (нашёл Playwright, а не глаз): части даты выводились из
>   `value`, но неполная дата отдаёт `""` — поэтому каждый следующий выбор стирал предыдущий и дата
>   **никогда** не могла собраться, кнопка «Отправить» оставалась серой. Теперь части живут в
>   `useState`, а `value` синхронизируется обратно только когда дата полная. Заодно: выбрать 31-е,
>   потом февраль → день **зажимается** до 28/29, а не улетает как «31 февраля».
>   Проверено в браузере: 31 + февраль 1999 → `dob: "1999-02-28"`, submit активен.
> - **Ошибки ловятся по статусу, а не по тексту** — бэкенд документирует их сам: 409 «userName /
>   email / phone уже заняты», 401 «Неверный логин или пароль (401, не 500)», 429 «Больше 5 запросов
>   в минуту». 401 намеренно разный по смыслу: на логине это «неверный пароль», в смене пароля —
>   «старый пароль неверен» (сказать «войдите заново» было бы враньём и потерей формы).
> - Проверено вживую (Playwright, стаб только на ответ сервера — БД лежит):
>   429 → «Too many attempts. Please wait a minute…», 401 → «The password you entered is incorrect…»,
>   409 → «That username, email or phone is already taken.»
> - `scripts/**` и `src/types/api.gen.ts` выведены из-под ESLint: первое — сборочный Node-скрипт
>   (CommonJS и `console.log` там по делу), второе — генерируется.
> - **Покрытие всего API: 81/170** (`docs/API_MAP_V2.md`). Это проводка, не работоспособность:
>   пока БД лежит, ни один ответ живьём не сверен.

## Фаза 13 — Профиль v2 (14 + 11 follow) ✅

- [x] `profile/edit` — **все** поля img43/img44: `fullName`, `website`, `occupation`, `dob`, `gender`, `about` + три тумблера (Threads / «Автор ИИ» / рекомендации)
- [x] **Закрытый аккаунт** (`PUT /profile/privacy`) + `PrivateAccountGate` — 403 это ворота, а не ошибка
- [x] **Синяя галочка** (`VerifiedBadge`) — профиль, посты, поиск, рекомендации, подписчики, заявки
- [x] Follow v2: **заявки** (принять/отклонить), **блок/разблок**, **«удалить подписчика»**, жалоба на аккаунт
- [x] **«Ваши действия»** (`/profile/me/activity`) + `settings/privacy`
- [x] `FollowButton` — берёт `is-following` (2 поля) вместо целого профиля на строку
- ✅ Готово: **follow 11/11**, **users 12/12**, **profile 12/14** — `saved-music` (Фаза 18) и
  `me/reposts` (хук `useMyReposts` есть, экрана нет)

> Заметки Фазы 13:
>
> - 🐛 **Найдено и починено: `defaultTheme="dark"` при `enableSystem`** — «Системная» тема **никогда не
>   работала**: у любого, кто не трогал переключатель, всегда была тёмная. Значит и все мои прошлые
>   «проверено в light» были на самом деле тёмными скриншотами. Теперь `defaultTheme="system"`;
>   проверено: `prefers-color-scheme: light` → `<html class="light">`, `body` = `rgb(255,255,255)`.
> - 🐛 **`gender` в форме остался старым**: `SelectItem value="Male"`, а enum теперь `MALE` — селект
>   никогда не совпадал со значением профиля. Плюс не хватало ключей `profile.fullName` и
>   `profile.dobHint` (в интерфейсе висело `profile.fullName`, Next показывал «2 Issues»).
>   Оба поймал браузер, не чтение кода.
> - ⚠️ **`/profile/me/activity` не пагинируется**: у `ActivityItemDto` нет своего `id`, а курсор — это
>   id последней строки. Берём одну страницу и не притворяемся, что есть «ещё».
> - **Блокировка несимметрична**: разблокировка **не возвращает** подписки (так пишет сам бэкенд), —
>   поэтому предупреждение стоит в ConfirmDialog **до** действия, а не после.
> - `reason` в жалобе — свободный текст (3–500), а не enum: шлём локализованную фразу, а не код `OTHER`.
> - Проверено вживую (Playwright, dark + light, стабы вместо лежащей БД): переключатель «Закрытый
>   аккаунт» = `checked` из профиля, 2 заявки с галочкой у `eraj`, «Разблокировать» в списке блока,
>   в `profile/edit` подтянулись `website`, `dob 1999-02-28`, «Prefer not to say», тумблер Threads.
>   Ошибок в консоли нет.

## Фаза 14 — Посты v2 (22) ✅

### Часть 1 ✅

- [x] `media[]` с типом/размерами/`thumbUrl` — карусель фото **и видео** (сделано в Фазе 11)
- [x] **Архив** (в архив / вернуть) — в меню «…» поста
- [x] «Кто лайкнул» (`PostLikesDialog`) — счётчик лайков открывает реальный список
- [x] **Поделиться** (`ShareSheet`): в чат (`toUserId`) / в историю (`toStory`) / **ссылка** — один endpoint
- [x] **Жалоба** на пост (`reason` — свободный текст, не enum)
- [x] `useUpdatePost` — правка подписи (хук готов, экрана правки ещё нет)
- ✅ **posts 22/22** endpoint'ов проведены

### Часть 2 ✅ (коммит `c7a0257`)

- [x] Создание: **локация** (img33, `LocationPicker`), **отметка людей** (`TagPeoplePicker`), `isReel`
      (пост только из видео уходит как рил)
- [x] **Хэштеги и @упоминания** — `RichCaption`: парсинг caption и комментариев, кликабельны
- [x] **Страница хэштега** `/explore/tags/[name]` — сделана здесь, а не отложена на Фазу 19:
      иначе ссылка вела бы в никуда. `search.service.ts` заведён целиком (остальные 3 — Фаза 19)
- [x] **`/u/[userName]`** — цель @упоминания (endpoint'а «username → id» нет, страница резолвит имя)
- [x] Комментарии: **ветки-ответы** и «Показать ответы (N)», ответ преднабирает @упоминание
- [x] Токен **`--ig-link`** (light + dark) → **99/170**

### Часть 3 ✅ — фаза закрыта полностью

- [x] Экран **правки подписи** (`EditCaptionDialog`, `PUT /posts/{id}`)
- [x] Сохранение в **коллекции** — long-press на закладке (жест IG) + контекстное меню
- [x] **Музыка** в создании поста (`MusicPicker`) + `music.service.ts` — с превью-плеером
- [x] **Фильтры** (img31): 9 пресетов IG, вкладка «Фильтры» — **круговой рейс** name → API → CSS
- [x] **Настройки** (img32): Яркость / Контраст / Насыщенность / Температура / Виньетка
- ✅ **Фаза 14 закрыта: posts 22/22**

> Заметки Фазы 14 (часть 3):
>
> - **Фильтр и «Настройки» хранятся по-разному, и это осознанно.** У медиа есть поле `filter`, поэтому
>   фильтр едет **именем** и применяется CSS-ом при показе — он остаётся редактируемым и виден так же,
>   как в превью. У ползунков img32 поля в API **нет вообще**, поэтому они **запекаются в пиксели**
>   при загрузке (`lib/crop.ts`, тем же canvas, что и кроп). Выдумывать поле, которого у бэкенда нет,
>   значило бы молча их терять.
> - Виньетку `context.filter` выразить не может (эффект зависит от координат, а не от пикселя) —
>   рисуется радиальным градиентом поверх.
> - `music.service.ts` заведён здесь, а не в Фазе 18: у поста есть `musicId`, и без поиска трека это
>   поле нечем заполнить. Плеер / «В тренде» / сохранённое — по-прежнему Фаза 18.
> - `collection` — это **имя**, а не id, и endpoint'а «список коллекций» нет, поэтому диалог просит
>   ввести имя, а не выбирать из того, что мы прочитать не можем.
> - 🐛 **Найдено в браузере: ✕ диалога перекрывал кнопку «Готово».** У shadcn `DialogContent` свой
>   крестик в правом верхнем углу — ровно там, где у моей шапки «Готово». Клик уходил в крестик,
>   и подпись **невозможно было сохранить**. `showCloseButton={false}` там, где шапка своя.
> - 🐛 **Моя прошлая заметка про «гонку с dev-сервером» была неверной.** Настоящая причина: стаб
>   `**/api/proxy/users**` ловил и `/users/suggestions`, а я отдавал туда `UserBriefDto` без
>   `followedBy` → `RightSidebar` падал и уносил всю страницу. Сломан был **тест, не приложение**.
>   Ветки-ответов, страница хэштега и `/u` теперь проверены на **production-сборке**.
> - Проверено (Playwright, `next start`, стабы вместо лежащей БД):
>   фильтр `clarendon` с сервера → на экране `contrast(1.2) saturate(1.35) brightness(1.1)`;
>   меню своего поста = `["Go to post","Share to story","Edit caption","Archive","Delete post"]`;
>   правка подписи → `PUT {"caption":"Новый текст #горы"}`; long-press → `{"collection":"Горы"}`;
>   `#travel` → `/explore/tags/travel`, `#душанбе` → закодирован, `@eraj` → `/u/eraj`;
>   `/u/eraj` при выдаче `[erajson, eraj]` уходит на **`/profile/u1`**, а не на первую строку.
>   Ошибок в консоли нет.

> Заметки Фазы 14 (часть 1):
>
> - **«Поделиться в историю» перестало быть подделкой.** Раньше это означало заново загрузить
>   картинку поста как новую историю (всё, что умел softclub). Теперь это один вызов
>   `POST /posts/{id}/share` с `toStory: true`, и сторис собирает сервер. `StoryUploadDialog` из
>   меню поста убран.
> - Ссылку выдаёт **сервер** (`ShareResultDto.link`) — не собираем её из `window.location`.
>   Если `navigator.clipboard` недоступен (http / без жеста), ссылка показывается в toast, а не
>   теряется молча.
> - Счётчик лайков теперь кликабельный: `/posts/{id}/likes` отдаёт людей, а не число.
> - 🐛 В генераторе карты нашлась дыра: `postService.report` был без generic (`http.post(...)`),
>   и регулярка его не видела — endpoint числился непроведённым, хотя вызов существовал.
>   Метод типизирован (`ReportCreatedDto`), карта снова честная.
> - Проверено вживую (Playwright, стабы): клик по «24 likes» → в списке `sadi`; «Copy link» уходит
>   с телом `{}` (только ссылка), «в историю» — `{toStory: true}`, «в чат» — `{toUserId}`.

> Заметки Фазы 14 (часть 2):
>
> - ⚠️ ~~Не доведены до конца: ветки-ответов, страница хэштега, резолвер `/u/` — «гонка с
>   dev-сервером»~~ — **это объяснение оказалось неверным**, см. заметки части 3: падал мой стаб,
>   а не приложение. Всё перепроверено на production-сборке и работает.
> - **`@упоминание` не может вести прямо в профиль:** endpoint'а «username → id» в API нет
>   (проверено по всему swagger: только `check-username` и `search/hashtag/{name}`), а роуты профиля
>   на uuid. Отсюда `/u/[userName]` — резолвит через `/users?q=` и форвардит. У IG URL тоже
>   `/username`, так что лишний шаг не виден.
> - ⚠️ **Резолвер обязан брать точное совпадение**: `/users?q=` ищет **подстроку** и по `userName`,
>   и по `fullName` («er» → «eraj», «america»), поэтому первая строка выдачи открыла бы чужой
>   профиль. И `replace`, а не `push`, иначе «Назад» с профиля возвращало бы обратно в резолвер.
> - 🐛 **Доступность:** `{sigil}{trimmed}` создавал **два** текстовых узла, и accessible name
>   становился «# travel» — скринридер читал решётку отдельным словом. Стало одной строкой.
> - `RichCaption` — Unicode-осознанный (`\p{L}\p{N}`), иначе `#душанбе` не распознавался бы.
> - `--ig-link`: `#0095f6` в размере текста на белом читается плохо, IG берёт более тёмный синий
>   (`#00376b` в light, `#e0f1ff` в dark). HEX — только в `globals.css`.

## Фаза 15 — Уведомления (5) ✅ — экраны img26–img28, которые нечем было делать

- [x] `NotificationsPanel` на реальных данных: **23 типа**, группировка («X и ещё 12»)
- [x] `GET /notifications/profile-views` — «кто заходил в профиль» (`/profile/views`)
- [x] **Счётчик непрочитанных** на сердце (`unread-count`, поллинг 30 сек) + «Отметить все прочитанными»
- [x] Роутинг строки по `postId` / `storyId` / иначе профиль актора
- ✅ **notifications 5/5**

> Заметки Фазы 15:
>
> - **Группирует сервер, не мы.** Строка — это последнее уведомление группы: `othersCount` — сколько
>   ещё людей за ним, `groupIds` — на что распространяется «прочитано». Клиент ничего не
>   дедуплицирует, он только формулирует фразу.
> - ⚠️ **Готовый `message` с сервера намеренно НЕ используется** (только как fallback для типа,
>   которого этот билд ещё не знает): он на одном языке, а правило проекта — каждая строка из
>   `messages/{en,ru,tg}.json`. Текст собирается из `type` + `actor` + `othersCount`, по одной
>   ICU-строке на тип: `{actor}{others, plural, =0 {} other { и ещё #}} оценил(а) вашу публикацию.`
>   23 типа × 3 языка.
> - 🐛 **Красная точка на сердце была декорацией и врала при каждом рендере** — её рисовали
>   безусловно с Фазы 3, потому что endpoint'а уведомлений не существовало. Теперь это реальный
>   счётчик (99+ при переполнении), и при нуле её просто нет.
> - ⚠️ **`/notifications/profile-views` не пагинируется**: у `ProfileViewDto` нет своего `id`, а
>   курсор — это id последней строки. Одна страница, без притворства, что есть «ещё».
> - Панель не запрашивает данные, пока её не открыли (`enabled: open`).
> - Миниатюры поста в строке нет — уведомление не несёт картинку, и выдумывать её не стали.
> - Проверено на production-сборке (`next start`, стабы — БД лежит): бейдж `7`;
>   группа → «eraj and 12 others liked your post»; одиночное → «sadi started following you» с кнопкой
>   «Подписаться»; «Отметить все» → `POST /read-all`; `/profile/views` → `zarina`. Ошибок нет.

## Фаза 16 — Stories v2 + Highlights + Close friends (12 + 5 + 3) — 20/20 endpoints ✅, музыка/стикеры в истории — нет

### Часть 1 ✅ — близкие друзья + реакции/ответы на историю

- [x] `closeFriends.service.ts` + `useCloseFriends` — **close-friends 3/3**
- [x] `settings/close-friends` — поиск, добавить/убрать (оптимистично, без подтверждений)
- [x] `StoryReplyBar` — **ответ** и **реакция** на чужую историю → уходят в чат (`stories 11/12`)
- [x] Ответ/реакция **ставят слайд на паузу** — иначе история уезжает, пока печатаешь
- ✅ **close-friends 3/3**, `stories/{id}/reply` + `/reaction`

### Часть 2 ✅ — Highlights + зелёное кольцо

- [x] **Highlights 5/5**: кружки в профиле, создание из архива, просмотр, удаление
- [x] **Зелёное кольцо** `hasCloseFriends` в рейле + токен `--ig-close-friends`
- [x] **`closeFriendsOnly`** — тумблер при загрузке истории
- [x] Архив используется как источник для «Актуального» (`useStoryArchive`)
- ✅ **highlights 4/5** — `PUT /highlights/{id}` (переименование/состав) хук есть, UI нет

### Часть 3 ✅ — архив историй, фаза закрыта

- [x] **`stories/archive`** (img45): «Архивировать» → таб «Истории», сетка 4×9:16, штамп даты
- [x] **`GET /stories/{id}`** — открытие плитки архива (**stories 12/12**)
- [x] Кнопка «Показать архив» в своём профиле ведёт **в архив**, а не в «Сохранённое»
- ✅ **stories 12/12**, **close-friends 3/3**, **highlights 4/5**

> Заметки Фазы 16 (часть 3):
>
> - 🐛 **«Показать архив» в профиле вела в «Сохранённое»** — кнопка называлась одним, а делала
>   другое, потому что архива у softclub не существовало и вести было некуда. Теперь ведёт туда,
>   как называется.
> - **Штамп даты**: год показывается только для истории из **другого** года — иначе дата
>   неоднозначна, а в img45 видны обе формы. Проверено: `30 Jun` (этот год) и `20 Mar 2025`.
> - `GET /stories/{id}` нашёл своё место именно здесь: строки сетки хватает на плитку, но при
>   открытии нужна вся история (медиа, музыка, оверлеи).
> - Таб один — отдельных endpoint'ов для архива постов/эфиров нет, поэтому вкладок больше не рисуем.
> - Проверено на production-сборке (стабы): 2 плитки, штампы `30·Jun` / `20·Mar·2025`,
>   клик → уходит `GET /stories/11`, заголовок диалога `June 30, 2026`. Ошибок нет.

> Заметки Фазы 16 (часть 2):
>
> - **Просмотрщик «Актуального» — намеренно НЕ `StoryViewer`.** У актуального нет 5-секундного
>   таймера и нет «просмотрено»: истории там лежат специально, поэтому их листают руками.
>   `/highlights/{id}` — единственное место, откуда приходят сами истории.
> - **Пикер читает архив + живые истории**: актуальное собирается из того, что уже было, а к этому
>   моменту большинство историй уже истекли. Живые идут первыми — их только что выложили.
> - **Обложка = первая выбранная история** (`coverUrl` опционален). Номер на плитке показывает
>   порядок и заодно кто станет обложкой — просить загрузить картинку до того, как есть что
>   покрывать, было бы странно.
> - ⚠️ **id актуального — строка (uuid)**, в отличие от историй и постов, у которых `number`.
> - «Просмотрено» **важнее зелёного**: просмотренная close-friends история сереет так же, как обычная.
>   Зелёный — один и тот же в обеих темах: это статус, а не поверхность.
> - Удаление актуального **не удаляет истории** — так пишет бэкенд, и текст подтверждения это говорит.
> - У закрытого аккаунта кружки не показываем — это те же истории.
> - Проверено на production-сборке (стабы): кружок «Кӯҳҳо» открывается, есть «Удалить»;
>   пикер показывает 3 плитки (1 живая + 2 из архива); создание уходит как
>   `{"title":"Сафар","storyIds":[10,12],"coverUrl":".../10.jpg"}`. Ошибок нет.

> Заметки Фазы 16 (часть 1):
>
> - **IG никого не уведомляет о близких друзьях** — ни при добавлении, ни при удалении. Поэтому
>   тумблер без confirm-диалога и оптимистичный: молчаливое действие должно быть мгновенным.
>   `add` идемпотентен, повтор — не ошибка.
> - Ответ и реакция на историю — это **сообщения в чат**, ровно как в IG. Реакция повторяемая
>   (`POST /stories/{id}/reaction`), ответ — `/reply`.
> - Панель ответа **замораживает таймер слайда** (`replying` в `StoryViewer`): без этого история
>   пролистывалась бы, пока человек печатает.
> - Панели нет на своей истории — отвечать самому себе нечему.
> - Проверено на production-сборке (стабы): ответ → `{"text":"Красиво!"}`, реакция → `{"emoji":"🔥"}`,
>   список близких друзей показывает `sadi` с «Убрать», поиск `zarina` → «Добавить» → `POST …/u7`.
>   Ошибок нет.

- [x] `isViewed` с сервера → кольца честные на любом устройстве _(Фаза 11: `story.store` удалён)_
- [x] **Список зрителей** (кто смотрел + лайкнул + реакция) — вместо двух счётчиков _(Фаза 11)_
- [x] **Реакции emoji** и **ответы** на историю → уходят в чат _(часть 1)_
- [x] **Close friends** — зелёное кольцо, `closeFriendsOnly` при загрузке _(части 1–2)_
- [x] **Актуальное (Highlights)** — создание, обложка, состав, кружки в профиле _(часть 2)_
- [x] **Архив историй** (img45) _(часть 3)_
- [ ] **Музыка и overlay-стикеры в истории** — единственный невыполненный пункт фазы:
      `musicId` / `musicStartSec` / `overlays` приняты в `story.service.ts`, но `StoryUploadDialog`
      их не шлёт. Музыка теперь возможна (`music.service` появился в Фазе 14), стикеры — это
      редактор поверх картинки, отдельная работа

## Фаза 17 — Чат v2 (20) — 🚧 16/20, сокет заблокирован

### Часть 1 ✅ — тема, никнейм, mute, реакции на сообщения

- [x] **Тема чата** (`ChatSettingsDialog`) — 6 пресетов, красит мои пузыри
- [x] **Никнейм** собеседника (только для меня) + **mute**
- [x] **Реакции на сообщения** — пикер по hover, повторный тап снимает свою реакцию
- ✅ **chats 16/20**

> Заметки: запросы на переписку (img22)
>
> - Экран, который Фаза 9 построить не могла: у softclub очереди запросов не было вовсе — любое
>   сообщение от незнакомца просто падало во «Входящие».
> - ⚠️ **`MessageRequestItemDto.id` — строка (uuid)**, в отличие от id чатов и сообщений (числа).
>   В сервисе стояло `acceptRequest(id: number)` — поймано до первого запуска.
> - «Скрытые запросы» из img22 **не рисуем**: endpoint'а под ними нет.
> - Красная «Удалить все N» есть (img22), но bulk-endpoint'а нет — она отклоняет каждый запрос по
>   очереди. Кнопка делает ровно то, что написано, просто N вызовов.
> - Принятие сразу уводит в чат: запрос превратился в обычную переписку.
> - Проверено на production-сборке (стабы): 2 строки, «Delete all 2», принять → `POST rq-1/accept`
>   и переход на `/chat/501`. Ошибок нет.

### Часть 2 — 🔴 сокет заблокирован бэкендом (не нашей ленью)

Сокет **живой**: `GET /socket.io/?EIO=4&transport=polling` → `0{"sid":"…","upgrades":["websocket"]}`
(в **корне домена**, `/api/socket.io/` = 404). Но подключить его нечем — две причины, обе на
стороне бэкенда, подробности и что просить: `docs/BACKEND_REQUEST.md` §4.

1. **Нечем авторизоваться.** Токен живёт только в httpOnly-cookie и в JS не попадает — это наше
   осознанное решение (XSS не украдёт). Сокет идёт на **другой origin**, cookie туда не уйдёт.
   Нужен `POST /auth/socket-ticket` (короткоживущий тикет) — его не страшно отдать JS.
2. **Не описаны события.** В Swagger нет ни одного имени: `message`? `newMessage`? какая комната,
   какой payload? Угадывать = писать код, который молча ничего не делает.

Пока этого нет — чат на опросе раз в 5 сек, а «Позвонить» не делаем: `POST /chats/{id}/call` без
сигналинга создаст звонок в никуда.

**Остальное части 2 (не заблокировано):**

- [x] Экран **запросов на переписку** (img22) — `chat/requests`, accept/decline
- [ ] UI редактирования сообщения (≤15 мин) — хук готов
- [ ] `bulk-delete` — нужен режим выбора нескольких сообщений
- [ ] Жалоба на чат, reply-на-сообщение, расшаренный пост в пузыре, стикеры

> ⚠️ **Карта API врала, и это исправлено.** Генератор считал endpoint «проведённым», если сервис
> вызывается из любого файла вне самого сервиса — **готовый хук без экрана этому условию
> удовлетворял**. Плюс `.getRequests(` матчил и `chatService`, и `followService` (совпадение имён),
> из-за чего `/chats/requests` числился сделанным благодаря хуку **follow**.
> Теперь: вызов должен идти из `components`/`app` напрямую либо через хук, который сам там
> используется; и метод сверяется вместе с именем сервиса. Честное число: **112/170**, а не 122.

## Фаза 18 — Музыка + Заметки (6 + 3 Spotify + 8) — 🚧 заметки готовы

### Часть 1 ✅ — Заметки (notes 6/8)

- [x] **Кружки над списком чатов** (`NotesRail`): текст ≤60, цвет фона, музыка, TTL 24ч
- [x] Создать / изменить / удалить свою (`NoteComposer` — одна заметка на человека)
- [x] **Лайк** заметки (toggle) и **ответ → сообщение в чат** автора
- ✅ **notes 6/8** → всего **122/170**

**Осталось:**

- [ ] `GET /notes/{id}/likes` и `/replies` — «кто лайкнул» / «ответы», **только автору**:
      хуки `useNoteLikes` / `useNoteReplies` готовы, экрана нет
- [ ] Плеер с `Range`-стримингом, «В тренде», сохранённое (`music` 2/6 — поиск и тренды уже
      работают внутри `MusicPicker`)
- [ ] **Spotify-поиск** (0/3)

> Заметки Фазы 18 (часть 1):
>
> - **Ответ на заметку — это не комментарий, а сообщение в чат автора.** Так в IG, и так в API:
>   `NoteReplySentDto` возвращает `chatId`, поэтому после отправки сразу уводим туда.
> - `bgColor` — **свободный hex**, не enum. Пресеты — только то, что предлагает пикер; цвет от
>   другого клиента отрисуется как есть.
> - Цвет текста считается по **относительной яркости** фона, а не подбирается на глаз: пикер
>   пропустит любой цвет, а нечитаемый текст хуже, чем отсутствие цвета.
> - Заметки живут 24ч и сервер отдаёт только активные — поэтому `refetchOnWindowFocus`, иначе в
>   кэше остался бы пузырь, которого уже нет. Поллинга не добавляли.
> - ⚠️ `/notes/{id}/likes` не пагинируется: у `NoteLikeItemDto` нет своего `id`, а курсор — это id
>   последней строки.
> - Проверено на production-сборке (стабы): свой пузырь и чужой рядом; ответ уходит
>   `{"text":"Зур!"}` и **переносит в `/chat/777`**; лайк → `notes/2/like`. Ошибок нет.

## Фаза 19 — Поиск v2 + Локации (4 + 5) — 🚧 search 3/4

- [x] **Комбинированный поиск** (`/search`): аккаунты + хэштеги + локации **одним ответом**
- [x] **Explore** переведён на `/search/explore` (микс фото/видео), а не на голый `/posts`
- [x] Страница хэштега — сделана в Фазе 14
- [x] **Хронологические «Недавние»** — то, что Фаза 8 объявила невозможным
- [x] Локации: **страница места** (`/explore/locations/{id}`), `locations` 5/5
- ✅ **search 3/4** → всего **123/170**

**Осталось:**

- [ ] **Тренды** (`GET /search/top`) — хук `useSearchTop` готов, экрана нет: в IG это то, что
      показывается в пустом поиске рядом с «Недавние»
- [ ] 🔴 **Посты места** — endpoint'а нет вовсе, см. заметку

> Заметки Фазы 19:
>
> - 🐛 **«Недавние» не были хронологическими, хотя уже могли.** Фаза 8 честно написала, что это
>   невозможно: у softclub история не имела времени, а два вида приходили из разных
>   последовательностей id (баг #14). В новом API **у обоих есть `createdAt`** — а код так и остался
>   «сначала аккаунты, потом запросы». Теперь один список, отсортированный по времени.
>   Проверено: аккаунт от 1 июля и запрос от 15-го → запрос сверху.
> - **Поиск отдаёт три вида одним ответом.** Фаза 8 умела искать только аккаунты
>   (`/User/get-users`) — хэштегов и мест у softclub не было в принципе.
> - 🔴 **У страницы места НЕТ сетки постов — и это не пропуск.** Endpoint'а «посты по локации» в API
>   нет: пост несёт свою `location`, но обратного запроса не существует, а `/search?q=` отдаёт сами
>   места, не их посты. Страница честно показывает место и говорит, что ленты нет. Запрошено в
>   `docs/BACKEND_REQUEST.md`.

## Фаза 20 — Верификация + Админка (4 + 4)

- [x] Экран «Meta Verified»: триал 7 дней, подписка $1000/мес (mock), отмена, `daysLeft`
- [x] Админ-панель (роль `ADMIN`): пользователи, **жалобы** (open/resolved)

## Фаза 21 — Live (16/18)

- [x] Начать эфир (`/live/start`), рейл «В эфире» в ленте историй, LIVE-кольцо на профиле
- [x] Зритель: join/leave, комментарии, сердечки (можно много раз), реакции-эмодзи
- [x] Хост: камера/звук (optimistic), список зрителей, кик, завершение → статистика
- [x] Заявка на участие (`/request-join`) — уходит; ответ хоста получить нечем
- [ ] **LiveKit-видео** — `wsUrl: ws://localhost:7880` указывает на машину зрителя
- [ ] **accept/decline заявки** — id заявки хосту не доходит ни одним путём
- [ ] **Звонки в чате** (`/chats/{id}/call`) — тот же сокет/WebRTC, что и §4

> Три дыры задокументированы в `docs/BACKEND_REQUEST.md` §7 (перенумеровано 19.07.2026 — §5
> «посты по локации» закрыт, добавился §6 про черновики):
>
> 1. `wsUrl` = `ws://localhost:7880` — для браузера это компьютер самого зрителя, видео прийти
>    не может ни с каким SDK. Экран показывает всё состояние эфира (зрители, лайки, камера,
>    статистика) и честно пишет, что видео не подключено. Подставной картинки нет.
> 2. `GET /live/{id}/comments` не существует — комментарий можно отправить, но поток чужих
>    не прочитать. Зритель видит только свои, и экран об этом пишет.
> 3. `accept`/`decline` берут id заявки, но списка заявок нет, а `NotificationDto` несёт
>    postId/commentId/storyId/noteId и **не несёт** requestId/liveId. Хост id не получает —
>    поэтому 2 из 18 не подключены и хук для них не написан.

---

## Swagger v3 (17.07.2026) — бэкенд ответил на `BACKEND_PROMPT.md`

Хост переехал: `kvv4` → **`a4k6`** (`kvv4` отдаёт 404, фронт всё это время смотрел в мёртвый бэкенд).
Swagger: **170 → 190**, удалённых endpoint'ов нет. Три из четырёх блокеров закрыты бэкендом:

| Было заблокировано | Чем закрыто                                                                 |
| ------------------ | --------------------------------------------------------------------------- |
| токен для сокета   | `POST /socket/ticket` — одноразовый тикет, httpOnly-cookie можно не трогать |
| WebRTC без ICE     | `GET /chats/calls/ice-servers` + `answer` / `decline` / `end`               |
| id заявки в эфир   | `GET /live/{id}/requests` + `requestId` и `liveId` в `NotificationDto`      |

🔴 **Но сокет всё ещё не собрать:** подключиться теперь есть чем, а **имён событий нет
нигде** — Swagger описывает только HTTP. В это упирается Фаза 22, а с ней и звонки:
`answer`/`decline`/`end` есть, но узнать о входящем нечем (`BACKEND_PROMPT.md` §3.2).

Ломающие изменения DTO разобраны в `API_REAL_DTO.md` (§ Swagger v3).

## Фаза 22 — Сокет + звонки (7) — 🔴 ЗАБЛОКИРОВАНА: имён событий нет

Тикет есть, ICE есть, `answer`/`decline`/`end` есть — но Swagger описывает только HTTP,
ни одного имени события (`message:new`, `call:incoming`, …) нигде нет. Угадывать не будем:
это гарантированно молчащий сокет. **До ответа бэкенда фазу не начинать.**

- [ ] `POST /socket/ticket` → подключение сокета, presence/typing/входящие события
- [ ] `GET /chats/calls/ice-servers` → WebRTC-конфиг
- [ ] `POST /chats/{id}/call` (уже в сервисе, кнопки нет) + `answer` / `decline` / `end`
- [ ] `GET /chats/{id}/calls` — история звонков; `MessageDto.call` рисует плашку звонка в ленте

## Фаза 23 — Групповые чаты (5)

- [ ] `POST /chats/group`, `PUT /chats/{id}/title`, `POST/DELETE /chats/{id}/participants`, `POST /chats/{id}/leave`
- [ ] `peer` у группы = `null` — типы и экраны уже это переживают (`chatLabel` / `chatAvatar`),
      но UI группы (список участников, админ-права, «вы вышли») ещё нет

## Фаза 24 — Внешний каталог музыки (3)

- [ ] `GET /music/online/providers`, `GET /music/online`, `POST /music/online/save`
- [ ] `isFullTrack: false` → играет только 30-сек превью (`previewUrl`), `streamUrl` = `null`

## Фаза 25 — Хвосты (8) — 3/8

- [ ] `GET /live/{id}/comments` (чужие комментарии эфира), `GET /live/{id}/requests` + accept/decline
- [ ] `GET /profile/me/collections` — коллекции сохранённого
- [x] `GET /users/by-username/{userName}` — костыль `UserNameResolver` удалён, `/u/{userName}` резолвит напрямую
- [x] `GET /locations/{id}/posts` — лента локации: `LocationScreen` теперь показывает грид, а не
      «нет ленты» (закрыто 19.07.2026, в ту же сессию что и Swagger v4)
- [ ] `GET /notes/{id}`
- [ ] `NoteDto.audience` (`FOLLOWERS` / `CLOSE_FRIENDS`) — переключатель в `NoteComposer`

## Swagger v4 (19.07.2026) — бэкенд снова вырос: 190 → 230

Ни одного endpoint'а не удалено, диф только добавляет: 2FA + управление сессиями, аналитика
(посты/истории/аккаунт), черновики и отложенная публикация постов, закреп поста и закреп
комментария, приватность лайков/комментариев на пост, коллаборативные посты (соавторы), ремиксы
reels, ревью отметок людей на постах, «Add Yours» цепочки историй, интерактивные стикеры
(опрос/вопрос/квиз) в историях, vanish mode и «просмотр один раз» в чате, общие настройки аккаунта
(тег `settings`, закрыт в этой же сессии) и один объединённый `GET /feed`.

Ни у одного из новых экранов нет скриншота в `docs/screenshots/` — это функциональность за
пределами исходного ТЗ. Полный постатейный список — `docs/API_MAP_V2.md` (строки `[ ]`).
Планируемая раскладка по фазам:

- **Фаза 26 — Безопасность (7) ✅:** `auth/2fa/*` (setup/enable/disable/verify) + `auth/sessions`
  (список / завершить одну / выйти со всех устройств кроме текущего) — см. § Фаза 26
- **Фаза 27 — Посты pro (14) ✅:** черновики + отложенная публикация, закреп поста (максимум 3) и
  закреп комментария, приватность поста (скрыть лайки/комментарии), соавторы (приглашение +
  accept/decline), ремиксы, ревью отметок людей, аналитика поста — см. § Фаза 27
- **Фаза 28 — Истории pro (7) ✅:** «Add Yours» (создать промпт + лента цепочки), аналитика истории,
  интерактивные стикеры (добавить / прочитать / ответить / итоги — 4 endpoint'а) — см. § Фаза 28
- **Фаза 29 — Групповые чаты (5) ✅:** не заблокирована (в отличие от Фазы 22) — `chats/group`,
  участники, переименование, выход — см. § Фаза 29
- **Фаза 30 — Приватность чата (3) ✅:** vanish mode (`PUT /chats/{id}/vanish`, `POST …/close`) и
  «просмотр один раз» (`POST /chats/messages/{id}/open`) — обычный REST, сокет не нужен —
  см. § Фаза 30
- **Фаза 31 — Единая лента (1) ✅:** `GET /feed` оказался дублем `posts/feed`, а не отдельной
  лентой — см. § Фаза 31
- **Фаза 25, хвосты-2 (7) ✅:** `live/{id}/comments` + `live/{id}/requests` (+accept/decline),
  `profile/me/collections`, `profile/me/insights`, `notes/{id}`, `notes/{id}/reaction`,
  `music/{id}/reels` — см. § Фаза 25 (хвосты-2)
- **Фаза 22 (не трогать, как и было):** сокет остаётся заблокирован — имён событий по-прежнему
  нет нигде в Swagger

## Фаза 27 — Посты pro (14) ✅ — 19.07.2026

- [x] **Закреп поста** (`PATCH /posts/{id}/pin`, максимум 3, сервер сам считает) — пункт в меню
      «…» своего поста (`PostHeader`), значок канцелярской кнопки на плитке грида
      (`PostGrid`/`ProfileView` — закреплённые сортируются первыми на клиенте)
- [x] **Закреп комментария** (`PATCH /posts/{postId}/comments/{id}/pin`, только автор поста) —
      пункт «Закрепить»/«Открепить» у корневого комментария, список сортируется закреплёнными
      вперёд на клиенте (`CommentList`)
- [x] **Приватность поста** (`PATCH /posts/{id}/privacy`): скрыть число лайков и выключить
      комментарии — два независимых пункта в том же меню «…»
- [x] **Соавторы**: приглашение — новый `CollaboratorPicker` на шаге подписи в `CreatePost`
      (второй вызов после создания поста, т.к. у `POST /posts` нет своего поля под это); принять/
      отклонить — `PendingCollabsBanner` над своей лентой постов (нет уведомления такого типа в
      `NotificationDto`, поэтому не в панели уведомлений, а прямо на профиле)
- [x] **Ремиксы** (`GET /posts/{id}/remixes`) — «Показать ремиксы» в меню reel'а, диалог с гридом
- [x] **Аналитика поста** (`GET /posts/{id}/insights`) — «Посмотреть статистику», диалог: охват,
      лайки/комменты/сохранения/шеры, engagement rate, подписчики/не подписчики, источники
- [x] **Ревью отметок людей** (`GET /posts/tags/pending` + `tag/accept|decline`) — новый экран
      `/settings/tag-review`, вход через «Метки и упоминания» → «Проверить метки» (раньше это была
      неактивная строка «Одобрять метки вручную» — такого поля в API вообще нет, переименовано в
      честную ссылку на реальный список)
- [x] **Черновики / отложенные посты** (`GET /posts/drafts` + `PUT /posts/{id}/publish`) — экран
      `/settings/drafts`, вход из «Архивация» → «Черновики»
- ✅ **posts 35/36** — единственный пропуск тот же, что всегда: `GET /posts` (дубль `/search/explore`)

> Заметки Фазы 27:
>
> - 🔴 **«Черновики» гарантированно пустой экран** — и это задокументированный пробел бэкенда, не
>   недоделка фронта. `POST /posts` не принимает ни `status`, ни `scheduledAt` — ничто в
>   документированном API не может создать пост со статусом `DRAFT`/`SCHEDULED`. Экран честно
>   работает (список + «Опубликовать сейчас»), но заполнить его нечем. Запрошено в
>   `docs/BACKEND_REQUEST.md` §6.
> - **Приглашения в соавторы — не уведомление.** У `NotificationDto` 23 типа, ни один не про
>   соавторство. Раньше это значило бы «выдумать баннер» — вместо этого баннер стоит именно там,
>   где приглашённый и так окажется: над собственной лентой постов.
> - **`hideLikeCount`/`commentsDisabled` — булевы, а не enum** — поэтому каждый пункт меню читает
>   текущее состояние с самого поста (`post.hideLikeCount`) и просто инвертирует его, как уже
>   делает «Архивировать».
> - `LikeShareCountsSettings` (глобальный тумблер в Settings) **сознательно не подключён** к
>   `hideLikeCount`: поле живёт на посте, а не на аккаунте — глобального дефолта в API нет, и
>   выдавать один пост за все значило бы врать.
> - Закреп сортируется **на клиенте** (`.sort` по `pinnedAt` перед рендером), а не ожидается от
>   сервера отсортированным — так же, как уже сделано для непагинируемых списков в `lib/cursor.ts`.

## Фаза 28 — Истории pro (7) ✅ — 19.07.2026

- [x] **Аналитика истории** (`GET /stories/{id}/insights`, только автору) — кнопка в шапке своей
      истории, диалог: просмотры/лайки/реакции/ответы + engagement rate
- [x] **«Add Yours»**: запустить цепочку на своей истории (`POST /stories/{id}/add-yours`,
      диалог текст+эмодзи), значок цепочки на любой истории с `addYoursPromptId`
      (`GET /stories/add-yours/{promptId}` — экран `/stories/add-yours/[promptId]`, грид ответов
      в стиле `StoryArchiveScreen`)
- [x] **Интерактивные стикеры** — создание (`POST /stories/{id}/stickers`), просмотр
      (`GET /stories/{id}/stickers`, отдельный вызов на слайд — не часть `StoryDto`), ответ
      (`POST …/stickers/{id}/answer`), итоги для автора (`GET …/stickers/{id}/results`)
- ✅ **stories 19/19**

> Заметки Фазы 28:
>
> - 🔵 **В редакторе создания — только POLL/QUESTION/SLIDER**, сознательно: QUIZ (нужен свой поток
>   «правильный ответ»), COUNTDOWN и LINK (только verified) не выведены в композитор — это
>   отмеренное сокращение, не недосмотр, и оно того же рода, что уже принят у текстовой подписи
>   истории (фиксированная позиция вместо перетаскивания). **Просмотр и ответ работают на все 6
>   типов** — если бэкенд когда-нибудь отдаст QUIZ/COUNTDOWN/LINK через другой путь, `StoryStickerLayer`
>   их уже отрисует.
> - **Расположение стикера фиксировано** (центр слайда), как и у текстовой подписи — полноценный
>   drag-canvas остаётся отдельной работой (см. заметку Фазы 16).
> - **POLL/QUIZ не показывают проценты зрителю** — только автору через «Итоги»
>   (`GET /stickers/{id}/results`). У обычного зрителя такого поля нет ни в `StickerDto`, ни где-либо
>   ещё, так что рисовать проценты всем значило бы выдумывать данные.
> - **COUNTDOWN и LINK — только отображение**, без вызова `/answer`: ни одно из трёх полей
>   `AnswerStickerDto` (`optionIndex`/`text`/`sliderValue`) не описывает «поставить напоминание» или
>   «перешёл по ссылке», так что выдумывать вызов, который ничего не значит, не стали.
> - `StoryDto` не несёт автора (рейл/просмотрщик группируют истории по `userId` отдельно) — поэтому
>   в ленте «Add Yours» диалог одной истории озаглавлен датой, как в `StoryArchiveScreen`, а не именем.

## Фаза 26 — Безопасность (7) ✅ — 19.07.2026

- [x] **2FA — включить**: `POST /auth/2fa/setup` (секрет + otpauth-URI) → код из приложения →
      `POST /auth/2fa/enable` → резервные коды (показаны один раз) — экран `/settings/two-factor`
- [x] **2FA — выключить**: `POST /auth/2fa/disable` (код или резервный)
- [x] **Второй шаг логина**: `POST /auth/2fa/verify` — `LoginForm` показывает экран кода, если
      `login` вернул тикет вместо токенов
- [x] **Сессии**: `GET /auth/sessions` (список устройств, «это устройство» отмечено),
      `DELETE /auth/sessions/{id}` (завершить одну), `POST /auth/sessions/logout-all` (выйти
      везде кроме текущего) — экран `/settings/login-activity`
- ✅ **auth 18/18** — весь тег полностью проведён

> Заметки Фазы 26:
>
> - 🔴 **Ответ `/auth/login` для 2FA-аккаунта нигде не задокументирован** — Swagger объявляет только
>   `TokensDto`, без ветки для тикета. Форма `{ twoFactorRequired: true, ticket }` — **предположение**,
>   собранное по описанию `Verify2faDto.ticket` ("выдан при логине"). Код различает ответы по
>   `"ticket" in response`, а не по имени флага — так неверное имя флага ничего не сломает. Живьём
>   проверить не удалось (нужен аккаунт с уже включённой 2FA). Подробности — `API_REAL_DTO.md`
>   § Swagger v4.
> - 🔴 **`GET /auth/sessions` и `POST /auth/sessions/logout-all` не могут идти через общий
>   `/api/proxy`** — оба требуют реальный refresh-токен (query `?rt=` и тело `{refreshToken}`
>   соответственно), а токен живёт только в httpOnly-cookie и никогда не должен попадать в
>   клиентский JS. Поэтому у них два выделенных серверных route'а (`app/api/auth/sessions/*`),
>   которые читают cookie сами и не отдают значение наружу. `DELETE /auth/sessions/{id}` токена
>   не требует — идёт через обычный `authService` и общий прокси, как всё остальное.
> - **Нет endpoint'а «включена ли 2FA»** — `AuthUserDto` такого поля не несёт. Экран
>   `/settings/two-factor` не может показать реальное состояние при открытии, только то, что
>   сделано в рамках текущего визита; `enable2fa`/`disable2fa` при этом работают корректно
>   независимо от того, что показывал экран.
> - **QR-код не рисуется** — в стеке нет библиотеки для этого; `otpauthUri` и секрет показаны как
>   текст для ручного ввода.
> - `user-agent`, обязательный заголовок `/auth/2fa/verify`, ничего специального не потребовал:
>   он идёт через обычный клиентский вызов, а общий `/api/proxy` и так пересылает все заголовки
>   входящего запроса, кроме явно вырезанных (`host`/`connection`/`content-length`/`cookie`).

## Фаза 29 — Групповые чаты (5) ✅ — 19.07.2026

- [x] **Создание группы** (`POST /chats/group`, 2–32 участника, создатель — админ) — «Новое
      сообщение» теперь мультивыбор: выбран 1 человек → обычный чат (как раньше), 2+ → поле
      названия группы и групповой чат
- [x] **Переименование** (`PUT /chats/{id}/title`, любой участник)
- [x] **Добавить участников** (`POST /chats/{id}/participants`, любой участник) —
      `GroupParticipantPicker`, тот же поиск, что и в «Новом сообщении»
- [x] **Удалить участника** (`DELETE /chats/{id}/participants/{userId}`, только админ)
- [x] **Выйти из группы** (`POST /chats/{id}/leave`, любой участник)
- ✅ **chats 24/33** — все групповые endpoint'ы подключены; оставшиеся 9 (звонки/ICE/сокет,
  vanish/view-once) — Фазы 22 и 30

> Заметки Фазы 29:
>
> - **«Новое сообщение» стало чекбоксами, а не радио** — img15 показывает именно радио-кнопку,
>   потому что групп на момент той фазы не существовало (API был строго 1-к-1). Групповой шаг
>   (поле названия) своего скриншота не имеет — то же самое «нет референса», что и у `StoryViewer`.
> - **Создание группы НЕ идемпотентно**, в отличие от обычного чата: два вызова с одним и тем же
>   составом участников — это две разные группы. Сервис и заметка это подчёркивают, чтобы никто не
>   попытался сделать «создать или найти» для групп.
> - `ChatListItemDto`/`ChatDetailDto` уже несли `isGroup`/`title`/`participants`/`isAdmin` со
>   Swagger v3 (17.07.2026) — `chatLabel`/`chatAvatar` в `chat.types.ts` и «N участников» в
>   `ChatWindow` уже были готовы принять группу, не хватало только самих действий.
> - **Удалить участника может только админ** — но админ всегда один (создатель), так что в списке
>   участников кнопка «Удалить» просто не показывается рядом со значком «Админ».

## Фаза 30 — Приватность чата (3) ✅ — 19.07.2026

- [x] **Vanish mode** (`PUT /chats/{id}/vanish`) — тумблер в `ChatSettingsDialog`
- [x] **Закрыть чат** (`POST /chats/{id}/close`) — вызывается сам, при уходе с экрана чата
      (`ChatWindow` unmount/смена `chatId`), сжигает увиденные исчезающие сообщения
- [x] **«Просмотр один раз»** (`POST /chats/messages/{id}/open`) — плейсхолдер «Нажмите, чтобы
      посмотреть» в `MessageBubble`, открывшему медиа показывается один раз, дальше — «Просмотрено»
- ✅ **chats 27/33** — оставшиеся 6 (звонки/ICE/сокет) — Фаза 22, заблокирована

> Заметки Фазы 30:
>
> - 🔴 **Отправить «просмотр один раз» нечем** — тот же паттерн, что черновики в Фазе 27:
>   `POST /chats/{id}/messages` не принимает поле `viewOnce` ни в multipart, ни в JSON-варианте.
>   Открытие уже присланного `viewOnce`-сообщения работает полностью; создать такое сообщение
>   через composer нельзя. Запрошено в `docs/BACKEND_REQUEST.md` §8.
> - **Нет поля «включён ли vanish mode сейчас»** — ни на `ChatDetailDto`, ни на `ChatListItemDto`.
>   Тот же гэп, что у 2FA (Фаза 26): тумблер в `ChatSettingsDialog` открывается всегда с «выключено»
>   и отражает только то, что переключено в рамках этого визита.
> - **`close` вызывается всегда**, не только когда vanish mode включён — если исчезающих сообщений
>   нет, сервер просто отвечает `{ deleted: 0 }`. Дешевле, чем городить локальное отслеживание
>   состояния vanish ради условного вызова.
> - **Реальное медиа `viewOnce` хранится только в компоненте**, не в кэше query: по контракту API
>   повторный `GET` вложение прячет для всех, кроме автора, так что кэшировать открытый URL значило
>   бы держать копию данных, которых по правилам уже не должно быть видно.

## Фаза 25 (хвосты-2) (7) ✅ — 19.07.2026

- [x] **Комментарии эфира от всех** (`GET /live/{id}/comments`) — `LiveCommentBar` больше не
      ограничен своими же сообщениями (были в локальном `mine`), опрос как у остального `live`
- [x] **Заявки на участие в эфире** (`GET /live/{id}/requests` + `accept`/`decline`, которые уже
      были в сервисе, но без UI) — `LiveJoinRequestsSheet`, кнопка у хоста рядом с камерой/звуком
- [x] **Мои коллекции** (`GET /profile/me/collections`) — чипы существующих коллекций поверх
      свободного поля в `SaveToCollectionDialog`, вместо запоминания названия на глаз
- [x] **Аналитика аккаунта** (`GET /profile/me/insights?period=`) — новый экран `/profile/insights`
      (прирост подписчиков, просмотры профиля, охват, вовлечённость, период 7/30/90 дней)
- [x] **Заметка по id** (`GET /notes/{id}`) — `NoteViewDialog`, открывается с уведомления
      (`NotificationDto.noteId`), а не с самой ленты `/notes` — заметка к этому моменту могла уже
      истечь (24ч)
- [x] **Эмодзи-реакция на заметку** (`POST /notes/{id}/reaction`) — раньше «быстрые эмодзи» в
      `NoteBubble` уходили как текстовый ответ (`reply` с текстом-эмодзи); теперь у них свой вызов
- [x] **«Use this audio»** (`GET /music/{id}/reels`) — счётчик «N публикаций» в `TrackRow` стал
      кликабельным, открывает `MusicReelsDialog` с гридом
- ✅ **live 20/20, music 10/10, notes 10/10, profile 16/16** — все четыре тега закрыты полностью

> Заметки Фазы 25 (хвосты-2):
>
> - **`accept`/`decline` заявок на эфир уже были в `live.service.ts`**, ждали только списка заявок
>   и экрана — сам вызов был готов с Фазы 21, просто нечем было получить id, на который отвечать.
> - **`NoteBubble`'s quick-emoji row слал текст, а не реакцию** — рабочий, но не тот вызов: поле
>   `text` принимало что угодно, включая один эмодзи, так что баг было не увидеть, не сверяя со
>   Swagger. Настоящая `reaction`-ручка появилась только 19.07.2026.
> - **`GET /notes/{id}` нашёл своё место не там, где ожидалось.** Изначально казалось — открытие
>   заметки из `NOTE_REPLY`-пузыря в чате; но `MessageDto` несёт `noteSnapshot` (текст), не id
>   заметки — открывать по факту нечего. Настоящий адресат — `NotificationDto.noteId`
>   (`LIKE_NOTE`/ответ), который таки несёт id.
> - **Коллекции читаются, но собственного экрана-грида не получили** — `FavoritesScreen` пиксель-
>   сверен с img36, где показан только плоский грид «Сохранённое», без папок-коллекций. Список из
>   `GET /profile/me/collections` подключён именно там, где сам эндпоинт называет его применение
>   («тот же `collection`, что принимает `/posts/{id}/favorite`») — как справочник имён в диалоге
>   сохранения, а не как ещё один грид без скриншота под него.
> - **`/profile/insights` и `insightsNav` в `SettingsNav`** — то же исключение из правила «только
>   по скриншоту», что уже сделано для `twoFactor`/`loginActivity` в Фазе 26: настоящий IG держит
>   аналитику аккаунта рядом с «Ваши действия», сюда она и легла.

## Фаза 31 — Единая лента (1) ✅ — 19.07.2026

- [x] **`GET /feed`** исследован и подключён — `useFeed` теперь зовёт `feedService.getFeed()`
      вместо `postService.getFeed()` (`/posts/feed`)
- [x] **`/posts/feed` задокументирован как второй сознательный дубль** в `API_MAP_V2.md`, рядом с
      `GET /posts` — вызывать оба значило бы дублировать один и тот же запрос
- [x] **Реальный пробел закрыт по пути**: `FeedDto.allCaughtUp`/`suggested` существовали с самого
      начала (Фаза 11/17.07.2026 — БД, из-за которой открылась разница со Swagger), но ни разу не
      читались нигде в UI — `Feed.tsx` теперь показывает баннер «Вы всё посмотрели» и рекомендованные
      посты после конца ленты подписок
- ✅ **feed 1/1**, `posts` теперь **34/36** (двойным дублем меньше в проводке, не в возможностях)

> Заметки Фазы 31:
>
> - **`GET /feed` и `GET /posts/feed` — не альтернативные ленты, а один и тот же endpoint дважды.**
>   Одинаковый `FeedDto`, одинаковые `cursor`/`limit`, дословно одинаковое описание ранжирования
>   (близость + свежесть + вовлечённость). Разница только в теге (`feed` — новый выделенный ресурс,
>   `posts` — старое место). Выбор в пользу `/feed`: похоже на направление, куда бэкенд мигрирует
>   ручку, а не произвольное предпочтение.
> - **`allCaughtUp`/`suggested` показываются только когда `hasNextPage` ложно** — иначе баннер и
>   рекомендации выскочили бы посреди ещё не догруженной ленты: сервер помечает `allCaughtUp` на
>   странице, где кончились непросмотренные, а не обязательно на последней странице подписок вообще.
> - `nextCursor()` из `lib/cursor.ts` тут не подошёл по той же причине, что и в `useAddYoursFeed`
>   (Фаза 28): `FeedDto.nextCursor` необязателен, а не просто nullable — `Page<T>` требует его
>   обязательным, так что оба места читают курсор вручную.

## Итог

| Модуль              | Было   | Стало   | Фаза      |
| ------------------- | ------ | ------- | --------- |
| auth                | 5      | 11      | 12        |
| profile+follow      | 11     | 26      | 13, 25    |
| posts               | 12     | 22      | 14        |
| notifications       | 0      | 5       | 15        |
| stories(+hl,cf)     | 8      | 20      | 16        |
| chats               | 6      | 30      | 17, 22–23 |
| music+notes+spotify | 0      | 21      | 18, 24    |
| search+locations    | 15     | 10      | 19, 25    |
| verification+admin  | 0      | 8       | 20        |
| live                | 0      | 20      | 21, 25    |
| socket              | 0      | 1       | 22        |
| users               | 9      | 13      | 13, 25    |
| upload+health       | 0      | 3       | 11        |
| **Всего**           | **57** | **190** | —         |

> Таблица — снимок исходной миграции softclub → NestJS (57 → 190) и не переписывается задним
> числом. Всё, что бэкенд добавил сверх этих 190 (19.07.2026: 190 → 230), разобрано в
> § Swagger v4 выше, отдельными фазами (26–31).

## Блокеры

- ✅ **БД поднялась — 17.07.2026, во второй половине дня.** `register` → 201 с реальной
  парой токенов, `login`, `chats`, `messages` — всё живое. Утром того же дня всё ещё
  было 500 `DATABASE_ERROR`, так что чинили на нашей памяти.

  **И правило «живой API авторитетнее Swagger» немедленно окупилось**, как и в 8 фазах
  из 10 на прошлом бэкенде: первый же живой ответ вскрыл, что **все cursor-списки
  отдают envelope `{items, nextCursor, hasMore}`, а Swagger объявляет голый массив**.
  Лента падала (`post.id === undefined`). Разбор и карта форм — `API_REAL_DTO.md`.

  Остальные DTO ниже Фазы 11 **всё ещё не сверены живьём** — теперь это наконец
  возможно, и стоит пройти по фазам заново.

- 🔴 **`storage: down`** — `/health` пишет «пайваст дар 3000 мс ҷавоб надод (timeout)».
  Загрузка медиа (`POST /upload`, аватар, пост, история) живьём не проверяется.
- ⚠️ **LiveKit / Spotify** требуют ключей и SDK вне зафиксированного стека (Фаза 21, 18).
- ⚠️ Render free tier — холодный старт ~50 сек на первый запрос.
</content>
